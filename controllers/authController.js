// Les vraies fonctions (register, login) seront implémentées juste après
module.exports = {
  register: async (req, res, next) => {
    return res.status(501).json({ message: 'register non encore implémenté' });
  },
  login: async (req, res, next) => {
    return res.status(501).json({ message: 'login non encore implémenté' });
  },
};
