# Configuration de l'API Book
# Copiez ce fichier en .env et modifiez les valeurs selon vos besoins

# Port d'écoute du serveur
PORT=4000

# Fichier de base de données SQLite
DB_FILE=database.sqlite

# Secret pour signer les tokens JWT (OBLIGATOIRE)
# ⚠️ IMPORTANT : Utilisez un secret fort et unique en production (au moins 32 caractères)
# Vous pouvez générer un secret avec : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=votre_secret_tres_long_et_complexe_au_moins_32_caracteres

# Durée de validité des tokens JWT
JWT_EXPIRES_IN=1h

# Niveau de logging (error, warn, info, debug)
LOG_LEVEL=info

# Environnement (development, production)
NODE_ENV=development
